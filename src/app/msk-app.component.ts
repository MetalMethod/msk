import { Component } from '@angular/core'

@Component({
    selector: 'msk-app',
    template: `
    <nav-bar></nav-bar>
    <router-outlet></router-outlet>
    `
})

//main app component
export class MskAppComponent{
    
}